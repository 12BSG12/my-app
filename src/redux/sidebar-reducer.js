let initialState = {
  friendsData: [
    {id: 1, name: 'Egor'},
    {id: 2, name: 'Alex'},
    {id: 3, name: 'Andrew'},
  ]
}

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;